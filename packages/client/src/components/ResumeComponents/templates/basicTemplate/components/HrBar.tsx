import { Box } from "@mui/material";
export const HrBar = () => {
  return (
    <Box
      sx={{
        borderTop: "1px",
        borderColor: "secondary.main",
        borderStyle: "solid",
      }}
    ></Box>
  );
};

export default HrBar;
